<!-- <%= link_to("ユーザー登録", "/user/new") %>
<%= link_to("ログイン", "/login") %> -->

<!-- _new.html.erbのテンプレートを表示 -->
<%= render ("new") %>

<% if logged_in? %>
<h3>現在のユーザー: <%= current_user.username %></h3>
<% end %>
<h1>タスク一覧</h1>

<% @task.each do |task| %>
  <div>
    <%= link_to( task.name ,"/task/show/#{task.id}") %>/
  </div>
<% end %>
